<UserControl x:Class="NewApiHelper.Views.UpstreamGroupView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:NewApiHelper.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/Styles/UpstreamManagementStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <!-- 全局加载指示器 -->
        <ProgressBar IsIndeterminate="True" VerticalAlignment="Top" Height="4"
                     Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}" />
        <DockPanel>
            <!-- 侧边栏布局：左侧分组列表，右侧详情编辑 -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="280" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <!-- 左侧：分组列表 -->
                <Grid Grid.Row="0" Grid.Column="0" Background="#FFF8F9FA">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="上游分组列表" FontSize="16" FontWeight="Bold" Margin="12,12,12,8" Foreground="#FF2E3B52" />
                    <ListView Grid.Row="1" ItemsSource="{Binding Groups}" SelectedItem="{Binding SelectedGroup, Mode=TwoWay}" Margin="6"
                              IsSynchronizedWithCurrentItem="True" Background="Transparent" BorderThickness="0">
                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="Margin" Value="6,2" />
                                <Setter Property="Padding" Value="8" />
                                <Setter Property="Background" Value="White" />
                                <Setter Property="BorderBrush" Value="#FFE0E0E0" />
                                <Setter Property="BorderThickness" Value="1" />
                                <Style.Triggers>
                                    <Trigger Property="IsSelected" Value="True">
                                        <Setter Property="Background" Value="#FFE6F7FF" />
                                        <Setter Property="BorderBrush" Value="#FF4A90E2" />
                                    </Trigger>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#FFF0F8FF" />
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </ListView.ItemContainerStyle>
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8">
                                    <StackPanel Orientation="Vertical">
                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="14" Foreground="#FF2E3B52" />
                                        <TextBlock Text="{Binding SelectedUpstream.Name}" FontSize="12" Foreground="Gray" Margin="0,2,0,0" />
                                        <TextBlock Text="{Binding GroupMultiplier, StringFormat='分组倍率: {0:F2}'}" FontSize="11" Foreground="#FF3BA1FF" Margin="0,2,0,0" />
                                        <TextBlock Text="新" Foreground="White" Background="#FF3BA1FF" Margin="0,4,0,0" Padding="6,2" FontSize="10" FontWeight="Bold" Visibility="{Binding IsNew, Converter={StaticResource BooleanToVisibilityConverter}}" HorizontalAlignment="Left" />
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                    <Button Grid.Row="2" Content="➕ 新增上游分组" Command="{Binding AddGroupCommand}" Margin="12" Style="{StaticResource ActionButtonStyle}" FontSize="14" Visibility="{Binding ShowAddButton, Converter={StaticResource BooleanToVisibilityConverter}}" />
                </Grid>
                <!-- 右侧：详情/编辑面板，使用 ContentControl 绑定 SelectedGroup -->
                <ContentControl Grid.Row="0" Grid.Column="1" Content="{Binding SelectedGroup}" Margin="12">
                    <ContentControl.ContentTemplate>
                        <DataTemplate DataType="{x:Type vm:UpstreamGroupItemViewModel}">
                            <Border BorderBrush="#FFE0E0E0" BorderThickness="1" CornerRadius="8" Padding="20" Background="White">
                                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                                    <StackPanel>
                                        <TextBlock Text="上游分组详情" FontSize="18" FontWeight="Bold" Foreground="#FF2E3B52" Margin="0,0,0,16" />
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                                            <Button Content="🔄 刷新" Command="{Binding DataContext.LoadGroupsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource SecondaryButtonStyle}" />
                                            <Button Content="➕ 新增" Command="{Binding DataContext.AddGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource ActionButtonStyle}" />
                                            <Button Content="📋 复制" Command="{Binding DataContext.CopyGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource ActionButtonStyle}" ToolTip="复制当前选中上游分组" />
                                            <Button Content="✏️ 编辑" Command="{Binding DataContext.StartEditGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource ActionButtonStyle}" ToolTip="选择一个上游分组以启用编辑" />
                                            <Button Content="🗑️ 删除" Command="{Binding DataContext.DeleteGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource DangerButtonStyle}" ToolTip="删除当前选中上游分组" />
                                            <Button Content="💾 保存" Command="{Binding DataContext.SaveGroupCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource ActionButtonStyle}" />
                                            <Button Content="❌ 取消" Command="{Binding DataContext.CancelEditCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" Style="{StaticResource SecondaryButtonStyle}" />
                                        </StackPanel>
                                        <Grid Margin="0,0,0,16">
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <StackPanel Grid.Column="0" Margin="0,0,12,0">
                                                <TextBlock Text="分组名称" Style="{StaticResource FormLabelStyle}" />
                                                <TextBox Text="{Binding Name, Mode=TwoWay}" IsEnabled="{Binding IsEditing}" Style="{StaticResource FormTextBoxStyle}" />
                                                <TextBlock Text="选择上游" Style="{StaticResource FormLabelStyle}" />
                                                <ComboBox ItemsSource="{Binding AvailableUpstreams}" SelectedItem="{Binding SelectedUpstream, Mode=TwoWay}" DisplayMemberPath="Name" IsEnabled="{Binding IsEditing}" Style="{StaticResource FormComboBoxStyle}" />
                                                <TextBlock Text="分组倍率" Style="{StaticResource FormLabelStyle}" />
                                                <TextBox Text="{Binding GroupMultiplier, Mode=TwoWay}" IsEnabled="{Binding IsEditing}" Style="{StaticResource FormTextBoxStyle}" />
                                                <TextBlock Text="Key" Style="{StaticResource FormLabelStyle}" />
                                                <TextBox Text="{Binding Key, Mode=TwoWay}" IsEnabled="{Binding IsEditing}" Style="{StaticResource FormTextBoxStyle}" />
                                            </StackPanel>
                                            <StackPanel Grid.Column="1" Margin="12,0,0,0">
                                                <TextBlock Text="创建时间" Style="{StaticResource FormLabelStyle}" />
                                                <TextBlock Text="{Binding CreatedAt, StringFormat='yyyy-MM-dd HH:mm:ss'}" FontSize="14" Foreground="#FF666666" Margin="0,0,0,8" />
                                            </StackPanel>
                                        </Grid>
                                    </StackPanel>
                                </ScrollViewer>
                            </Border>
                        </DataTemplate>
                    </ContentControl.ContentTemplate>
                </ContentControl>
            </Grid>
        </DockPanel>
    </Grid>
</UserControl>